# Spectools Waterfall payload configuration
# Loaded by payloads/spectools_waterfall/payload.sh

# Override source command for USB mode
# SOURCE_COMMAND="spectool_raw --list"

# Example net mode command
# SOURCE_COMMAND="spectool_raw --connect 127.0.0.1:30569"
